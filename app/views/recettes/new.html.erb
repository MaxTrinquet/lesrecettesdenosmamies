  <h1><strong>Votre Recette </strong></h1></div>

  <div class="container">
    <%= simple_form_for new_user_recette_path do |f| %>
      <%= f.error_notification %>
      <%= f.input :name %>
      <%= f.input :description %>
      <%= f.input :createur %>
      <%= f.input :ingredient %>
      <%= f.button :submit, class: "btn btn-primary font-weight-bold" %>
    <% end %>
  </div>
